{
  "name": "canto",
  "version": "1.0.0",
  "description": "A WaniKani-style spaced repetition app for learning Cantonese characters and vocabulary.",
  "private": true,
  "type": "commonjs",
  "main": "server/index.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev:server": "node --experimental-sqlite --watch server/index.js",
    "dev:client": "npm run dev --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "seed": "node --experimental-sqlite server/seed/seed.js",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node --experimental-sqlite server/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
